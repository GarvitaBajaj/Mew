<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="iiitd.gritlab.facultyapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
    </activity>/>
        <activity android:name="recruitment.iiitd.edu.mew.HomeScreen"
            android:parentActivityName=".MainActivity">
        <!--    <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter> -->
        </activity>
        <activity android:name="recruitment.iiitd.edu.mew.SettingsActivity"
            android:parentActivityName="recruitment.iiitd.edu.mew.HomeScreen"/>
        <activity android:name=".GraphCount" android:launchMode="singleTask"/>
        <receiver android:name=".FireQuery" />

    </application>

</manifest>